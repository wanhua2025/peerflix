# 📖 Peerflix Server 安装使用指南

## 📋 目录
- [系统要求](#系统要求)
- [安装步骤](#安装步骤)
- [启动服务](#启动服务)
- [使用方法](#使用方法)
- [配置说明](#配置说明)
- [常见问题](#常见问题)

---

## 💻 系统要求

### 必需软件
- **Node.js**: >= 6.0 (推荐使用 Node.js 12 或更高版本)
- **npm**: 随 Node.js 自动安装

### 可选软件
- **FFmpeg**: 用于视频转码功能（可选）

### 操作系统
- ✅ Windows 7/8/10/11
- ✅ Linux (Ubuntu, CentOS, Debian 等)
- ✅ macOS

---

## 🚀 安装步骤

### 方法一：从源码安装（推荐）

#### 1. 克隆或下载项目
```bash
# 如果有 Git
git clone https://github.com/asapach/peerflix-server.git
cd peerflix-server

# 或者直接解压下载的 ZIP 文件到某个目录
cd peerflix-server-0.6.0
```

#### 2. 安装 Node.js 依赖
```bash
npm install
```

#### 3. 安装构建工具
```bash
# 全局安装 Grunt 和 Bower
npm install -g grunt-cli bower

# 如果在 Windows 上遇到权限问题，使用管理员权限运行 PowerShell
```

#### 4. 安装前端依赖
```bash
bower install
```

如果 `bower install` 失败，可以尝试：
```bash
bower install --allow-root
```

#### 5. 构建前端资源
```bash
grunt build
```

这会编译、压缩和优化所有前端资源。

#### 6. 启动服务器
```bash
npm start
```

成功后会显示：
```
Listening on http://localhost:9000
```

---

### 方法二：全局安装（从 npm）

```bash
# 全局安装
npm install -g peerflix-server

# 直接运行
peerflix-server
```

**注意**：全局安装的版本可能不包含本次的美化和功能改进。

---

## 🎮 启动服务

### 开发模式（推荐用于测试）

```bash
# 启动开发服务器（带热重载）
grunt serve
```

特点：
- ✅ 自动打开浏览器
- ✅ 代码修改自动刷新
- ✅ 实时错误提示

### 生产模式

```bash
# 直接启动
npm start

# 或者
node server/bin.js
```

### 自定义端口

```bash
# Windows
set PORT=8080
npm start

# Linux/macOS
PORT=8080 npm start
```

### 后台运行（守护进程）

#### 使用 forever
```bash
# 安装 forever
npm install -g forever

# 启动守护进程
forever start node_modules/.bin/peerflix-server

# 或者如果是全局安装
forever start $(which peerflix-server)

# 查看日志
forever logs

# 停止服务
forever stop peerflix-server
```

#### 使用 PM2（推荐）
```bash
# 安装 PM2
npm install -g pm2

# 启动服务
pm2 start server/bin.js --name "peerflix-server"

# 查看状态
pm2 status

# 查看日志
pm2 logs peerflix-server

# 停止服务
pm2 stop peerflix-server

# 开机自启动
pm2 startup
pm2 save
```

---

## 📱 使用方法

### 访问界面

启动成功后，在浏览器打开：
```
http://localhost:9000
```

如果是在服务器上运行，替换 `localhost` 为服务器 IP 地址。

---

### 1️⃣ 添加种子

#### 方式一：磁力链接
1. 复制磁力链接（如：`magnet:?xt=urn:btih:...`）
2. 粘贴到输入框
3. 点击"开始下载"按钮

#### 方式二：种子文件
1. 点击"点击浏览上传"
2. 选择 `.torrent` 文件
3. 自动开始下载

#### 方式三：拖放上传
1. 直接将 `.torrent` 文件拖到输入区域
2. 自动开始下载

#### 方式四：InfoHash
1. 输入 40 位或 32 位的 InfoHash
2. 点击"开始下载"
3. 系统会自动转换为磁力链接

#### 方式五：直接访问 URL（新功能）
```
http://localhost:9000/torrents/{infoHash}/files
```
直接访问这个 URL，系统会自动添加并开始下载该种子。

---

### 2️⃣ 播放/下载文件

#### 在线播放
1. 等待种子元数据加载完成
2. 点击文件名链接
3. 使用浏览器内置播放器或第三方播放器（如 VLC、PotPlayer）

**支持的格式**：
- 视频：MP4, MKV, AVI, WebM 等
- 音频：MP3, FLAC, WAV 等
- 其他：任何可以流式传输的文件

#### 下载为 ZIP
1. 点击"下载为压缩包"
2. 浏览器会下载包含所有文件的 ZIP 压缩包

#### 生成播放列表
1. 点击"播放列表"
2. 下载 M3U 播放列表文件
3. 使用播放器（VLC、PotPlayer 等）打开

---

### 3️⃣ 文件管理

#### 选择性下载
- 勾选/取消勾选文件前的复选框
- 只下载选中的文件
- 节省磁盘空间和带宽

#### 全选/取消全选
- 点击顶部的"全部 X 个文件"复选框

#### 暂停/继续
- 点击"暂停"按钮停止上传和下载
- 点击"继续"按钮恢复
- **新功能**：暂停时会真正断开所有连接

#### 删除种子
- 点击右上角的 ❌ 按钮
- 种子及其文件会被删除

---

### 4️⃣ 查看统计信息

每个种子卡片底部显示实时统计：

- **速度**：⬇️ 下载速度 / ⬆️ 上传速度（KB/s）
- **剩余**：预计完成时间（分钟）
- **流量**：⬇️ 已下载 / ⬆️ 已上传（MB）
- **节点**：连接的活跃节点 / 总节点数
- **队列**：等待处理的数据块数量

---

## ⚙️ 配置说明

### 配置文件位置

```
Windows:  C:\Users\你的用户名\.config\peerflix-server\config.json
Linux:    ~/.config/peerflix-server/config.json
macOS:    ~/.config/peerflix-server/config.json
```

### 创建配置文件

第一次运行时配置文件不存在，可以手动创建：

```bash
# Linux/macOS
mkdir -p ~/.config/peerflix-server
nano ~/.config/peerflix-server/config.json

# Windows（PowerShell）
New-Item -Path "$env:USERPROFILE\.config\peerflix-server" -ItemType Directory -Force
notepad "$env:USERPROFILE\.config\peerflix-server\config.json"
```

### 配置选项示例

```json
{
  "connections": 100,
  "uploads": 10,
  "downloads": 10,
  "tmp": "D:/Torrents",
  "tracker": true,
  "trackers": [
    "udp://tracker.openbittorrent.com:80",
    "udp://tracker.opentrackr.org:1337"
  ],
  "dht": true,
  "verify": true
}
```

### 配置选项说明

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `connections` | 数字 | 100 | 最大连接数 |
| `uploads` | 数字 | 10 | 上传插槽数 |
| `downloads` | 数字 | 10 | 下载插槽数 |
| `tmp` | 字符串 | 系统临时目录 | 下载文件保存位置 |
| `tracker` | 布尔 | true | 是否启用 Tracker |
| `dht` | 布尔 | true | 是否启用 DHT |
| `verify` | 布尔 | true | 是否验证数据完整性 |

### 状态文件

系统会在配置目录保存当前状态：
```
~/.config/peerflix-server/torrents.json
```
这样重启后会自动恢复之前的下载。

---

## 🐳 Docker 部署

### 构建镜像

```bash
docker build -t peerflix-server .
```

### 运行容器

```bash
docker run -d \
  --name peerflix \
  -p 9000:9000 \
  -p 6881:6881 \
  -v /path/to/downloads:/tmp/torrent-stream \
  peerflix-server
```

### Docker Compose

创建 `docker-compose.yml`：

```yaml
version: '3'
services:
  peerflix:
    build: .
    ports:
      - "9000:9000"
      - "6881:6881"
    volumes:
      - ./downloads:/tmp/torrent-stream
    restart: unless-stopped
```

启动：
```bash
docker-compose up -d
```

---

## 🔧 高级使用

### REST API 调用

#### 添加种子
```bash
curl -X POST http://localhost:9000/torrents \
  -H "Content-Type: application/json" \
  -d '{"link":"magnet:?xt=urn:btih:..."}'
```

#### 获取种子列表
```bash
curl http://localhost:9000/torrents
```

#### 暂停种子
```bash
curl -X POST http://localhost:9000/torrents/{infoHash}/pause
```

#### 流式播放文件
```bash
curl http://localhost:9000/torrents/{infoHash}/files/{filePath}
```

完整 API 文档见：[REST.md](REST.md)

---

## 🔐 添加密码保护

### 使用 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        auth_basic "Restricted Access";
        auth_basic_user_file /etc/nginx/.htpasswd;
        
        proxy_pass http://localhost:9000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

创建密码文件：
```bash
sudo htpasswd -c /etc/nginx/.htpasswd username
```

---

## ❓ 常见问题

### 1. 端口已被占用

**错误**：`Port 9000 is busy`

**解决**：
```bash
# 使用其他端口
PORT=8080 npm start
```

### 2. 无法安装 Bower 依赖

**错误**：`bower install` 失败

**解决**：
```bash
# 使用 --allow-root（不推荐在生产环境）
bower install --allow-root

# 或者跳过 Bower，直接使用已构建的版本
npm start
```

### 3. 找不到 grunt 命令

**错误**：`grunt: command not found`

**解决**：
```bash
# 全局安装
npm install -g grunt-cli

# 或使用 npx
npx grunt build
```

### 4. 下载速度慢

**解决**：
- 检查防火墙是否开放端口 6881
- 尝试添加更多 Tracker
- 等待更多 peer 连接
- 检查网络连接

### 5. 无法播放某些视频格式

**解决**：
- 安装 FFmpeg
- 使用外部播放器（VLC、PotPlayer）
- 使用转码功能：`?ffmpeg=remux`

### 6. 种子一直显示"正在获取元数据"

**原因**：
- InfoHash 不存在或无效
- DHT 网络未连接
- 防火墙阻止连接

**解决**：
- 检查 InfoHash 是否正确
- 等待更长时间（可能需要几分钟）
- 使用包含 Tracker 的磁力链接
- 检查配置文件中的 `dht` 和 `tracker` 选项

### 7. 重启后种子消失

**原因**：配置目录不可写

**解决**：
- 检查 `~/.config/peerflix-server/` 目录权限
- 手动创建目录：`mkdir -p ~/.config/peerflix-server`

### 8. 浏览器无法访问

**检查**：
```bash
# 确认服务正在运行
netstat -an | grep 9000

# 或在 Windows 上
netstat -an | findstr 9000
```

**解决**：
- 检查防火墙设置
- 确认服务器 IP 地址
- 尝试使用 `0.0.0.0` 绑定所有接口

---

## 📊 性能优化

### 1. 增加连接数
```json
{
  "connections": 200,
  "uploads": 20,
  "downloads": 20
}
```

### 2. 指定 SSD 作为临时目录
```json
{
  "tmp": "/path/to/ssd/torrents"
}
```

### 3. 使用专用端口
确保 6881 端口在路由器上做了端口转发。

### 4. 关闭验证（不推荐）
```json
{
  "verify": false
}
```
这会加快速度但可能导致数据损坏。

---

## 🎯 使用技巧

### 1. VLC 播放器设置
- 打开 VLC
- 媒体 → 打开网络流
- 输入：`http://localhost:9000/torrents/{infoHash}/files/{fileName}`

### 2. PotPlayer 设置
- 右键 → 打开 → 打开链接
- 粘贴文件 URL

### 3. 搭配 aria2 下载
```bash
aria2c "http://localhost:9000/torrents/{infoHash}/files/{fileName}"
```

### 4. 远程访问
```bash
# 使用 ngrok 创建临时公网地址
ngrok http 9000
```

---

## 📞 获取帮助

- GitHub Issues: https://github.com/asapach/peerflix-server/issues
- 查看日志文件获取错误信息
- 使用 `--verbose` 选项查看详细输出

---

## 🎉 开始使用

现在您可以开始使用 Peerflix Server 了！

1. 启动服务：`npm start`
2. 打开浏览器：`http://localhost:9000`
3. 添加种子并开始播放！

**享受在线流媒体体验！** 🍿🎬
